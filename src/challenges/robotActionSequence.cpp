#include <iostream>
#include <vector>

/** @brief :  Given a string of combinations of G = Go, L = Left, R = Right ; determine if that sequence of actions will result in the robot moving in a circle forever. 
 * */
bool Solution(const std::vector<std::string> &v);

int main(int argc, char const *argv[])
{
    
    return 0;
}
